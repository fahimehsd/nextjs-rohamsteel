export interface NewsState {
  id: number;
  title: string;
  desc: string;
  image?: string;
}
export interface ProductsState {
  id: number;
  name: string;
  desc: string;
  image?: string;
  price: number;
}
export interface MoreState {
  id: number;
  title: string;
  desc: string;
  image?: string;
}
